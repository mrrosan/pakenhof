<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Webshop Restaurant</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
        <ul>
          <li><a href="/"><%=gettext "Home"%></a></li>
          <%= if WebshopRestaurantWeb.UserView.loggedin(@conn) do %>
          <%= if WebshopRestaurantWeb.UserView.is_admin(@conn) do %>
          <li><a href="<%= Routes.category_path(@conn, :index)%>"><%=gettext "Category"%></a></li>
          <li><a href="<%= Routes.order_path(@conn, :index)%>"><%=gettext "All Orders"%></a></li>
          <li><a href="<%= Routes.user_path(@conn, :index)%>"><%=gettext "Users"%></a></li>
          <% end %>
          
          <li><a href="<%= Routes.product_path(@conn, :index)%>"><%=gettext "Product"%></a></li>
          <%= if !WebshopRestaurantWeb.UserView.is_admin(@conn) do %>
              <li><a href="<%= Routes.order_path(@conn, :my_orders)%>"><%=gettext "My Orders"%></a></li>
          <% end %>
          <li><a href="<%= Routes.cart_path(@conn, :index)%>"><%=gettext "Cart"%></a></li>
          <li><a href="<%= Routes.session_path(@conn, :logout)%>"><%=gettext "Logout"%></a></li>
          <%= else %>
            <li><a href="<%= Routes.session_path(@conn, :new)%>"><%=gettext "Login"%></a></li>
          <% end %>

        </ul>
      </nav>
      <ul>
        <li><%= new_locale @conn, :en, gettext "English" %></li>
        <li><%= new_locale @conn, :nl, gettext "Dutch" %></li>
      </ul>
      
      <img src="<%= Routes.static_path(@conn, "/images/logotext.png") %>" alt="Brasserie Pakenhof Logo"/>

      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
