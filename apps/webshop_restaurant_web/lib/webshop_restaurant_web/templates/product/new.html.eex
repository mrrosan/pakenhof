<h1><%=gettext "New Product"%></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.product_path(@conn, :create)) %>

<span><%= link gettext("Back"), to: Routes.product_path(@conn, :index) %></span>
